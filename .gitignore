# Exclude packaged Helm charts (but allow them in gh-pages branch)
# Note: .tgz files should be tracked in gh-pages branch for the Helm repository
*.tgz
charts/*.tgz

# Exclude other common files
.DS_Store
*.log
node_modules/
.env
.env.local
.env.*.local

# Exclude temporary files
*.tmp
*.temp
