{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "aws-ec2-cloudwatch-alarms.serviceAccountName" . }}
  labels:
    {{- include "aws-ec2-cloudwatch-alarms.labels" . | nindent 4 }}
  annotations:
    {{- if .Values.global.env.aws.AWS_ROLE_ARN_EC2_CLOUDWATCH_ALARMS }}
    eks.amazonaws.com/role-arn: {{ .Values.global.env.aws.AWS_ROLE_ARN_EC2_CLOUDWATCH_ALARMS }}
    {{- end }}
    {{- with .Values.serviceAccount.annotations }}
    {{- toYaml . | nindent 4 }}
    {{- end }}
{{- end }}
