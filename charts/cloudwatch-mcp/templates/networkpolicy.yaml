{{- if .Values.networkPolicy.enabled -}}
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: {{ include "cloudwatch-mcp.fullname" . }}
  labels:
    {{- include "cloudwatch-mcp.labels" . | nindent 4 }}
spec:
  podSelector:
    matchLabels:
      {{- include "cloudwatch-mcp.selectorLabels" . | nindent 6 }}
  policyTypes:
    - Egress
  {{- if .Values.networkPolicy.egress }}
  egress:
    {{- toYaml .Values.networkPolicy.egress | nindent 4 }}
  {{- end }}
{{- end }}
