{{- if .Values.configMap.enabled }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "gcp-mcp.fullname" . }}-config
  labels:
    {{- include "gcp-mcp.labels" . | nindent 4 }}
data:
  # Add any additional configuration files here
  # Example:
  # config.yaml: |
  #   # Configuration content
{{- end }}
