{{- if .Values.networkPolicy.enabled }}
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: {{ include "incident-ingester.fullname" . }}
  labels:
    {{- include "incident-ingester.labels" . | nindent 4 }}
spec:
  podSelector:
    matchLabels:
      {{- include "incident-ingester.selectorLabels" . | nindent 6 }}
  policyTypes:
    - Egress
  {{- with .Values.networkPolicy.egress }}
  egress:
    {{- toYaml . | nindent 4 }}
  {{- end }}
{{- end }}
