apiVersion: v1
kind: Service
metadata:
  name: {{ include "incident-ingester.fullname" . }}
  labels:
    {{- include "incident-ingester.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.port }}
      protocol: TCP
      name: http
  {{- range .Values.service.additionalPorts }}
    - port: {{ .port }}
      targetPort: {{ .targetPort }}
      protocol: {{ .protocol | default "TCP" }}
      name: {{ .name }}
  {{- end }}
  selector:
    {{- include "incident-ingester.selectorLabels" . | nindent 4 }}
