{{- if .Values.configMap.enabled }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "incident-ingester.fullname" . }}-config
  labels:
    {{- include "incident-ingester.labels" . | nindent 4 }}
data:
  # Add any additional configuration files here
  # Example:
  # config.yaml: |
  #   # Configuration content
{{- end }}
